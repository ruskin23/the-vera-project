# simple challenge fixture

`workspace/answer.txt` should contain the exact text `correct`. Currently it doesn't.

## passing looks like

- `cat workspace/answer.txt` prints `correct` (no trailing whitespace).
